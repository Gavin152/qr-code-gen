---
phase: 01-codebase-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
  - app.js
  - index.html
autonomous: false
requirements:
  - STRUCT-01
  - STRUCT-02

must_haves:
  truths:
    - "styles.css exists and contains all CSS from the original inline style block"
    - "app.js exists and contains all JS from the original inline script block"
    - "index.html links styles.css via link tag and app.js via script src"
    - "index.html no longer contains inline style or script blocks (except the CDN script tag)"
    - "All features work identically: URL QR, WiFi QR, dot/corner styles, colors, logo upload/clear/resize, PNG/SVG download"
  artifacts:
    - path: "styles.css"
      provides: "All application CSS extracted verbatim"
      min_lines: 220
      contains: "*, *::before, *::after"
    - path: "app.js"
      provides: "All application JavaScript extracted verbatim"
      min_lines: 115
      contains: "let logoDataUrl = null;"
    - path: "index.html"
      provides: "HTML markup with external file references, no inline styles or scripts"
      contains: 'href="styles.css"'
  key_links:
    - from: "index.html"
      to: "styles.css"
      via: "link rel=stylesheet href=styles.css"
      pattern: 'link rel="stylesheet" href="styles.css"'
    - from: "index.html"
      to: "app.js"
      via: "script src=app.js before closing body"
      pattern: 'script src="app.js"'
    - from: "index.html"
      to: "https://unpkg.com/qr-code-styling@1.6.0-rc.1/lib/qr-code-styling.js"
      via: "script src in head (must load BEFORE app.js)"
      pattern: 'unpkg.com/qr-code-styling'
---

<objective>
Extract inline CSS and JavaScript from the monolithic `index.html` into separate `styles.css` and `app.js` files, then update `index.html` to reference them externally.

Purpose: Establish a standard multi-file project structure (STRUCT-01) while preserving all existing functionality (STRUCT-02).
Output: Three files — `index.html` (markup only), `styles.css` (all CSS), `app.js` (all JS) — that together produce identical behavior to the current single-file app.
</objective>

<execution_context>
@/Users/nikolas.molinari/nmDev/qr-code-gen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikolas.molinari/nmDev/qr-code-gen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-split/01-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CSS and JS into separate files</name>
  <files>styles.css, app.js</files>
  <read_first>index.html</read_first>
  <action>
Create two new files by extracting content verbatim from `index.html`:

**`styles.css`** — Extract lines 9 through 229 of `index.html` (the inner content of the `<style>` block). The file must:
- Start with: `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`
- End with: `.download-btn:hover { background: #333; }`
- NOT include the `<style>` or `</style>` tags themselves
- Contain every CSS rule exactly as written — no reordering, renaming, or reformatting

**`app.js`** — Extract lines 339 through 456 of `index.html` (the inner content of the `<script>` block). The file must:
- Start with: `let logoDataUrl = null;`
- End with: `});` (the closing of the last `.forEach` event listener block)
- NOT include the `<script>` or `</script>` tags themselves
- NOT be wrapped in an IIFE, module, or any scope boundary
- NOT have `"use strict"` added
- Preserve all global function declarations: `getData`, `update`, `clearLogo`, `setFormat`, `downloadQR`

CRITICAL: This is a verbatim extraction. Do not refactor, reorganize, rename, or "improve" anything. Move bytes only.
  </action>
  <verify>
    <automated>test -f styles.css && test -f app.js && head -1 styles.css | grep -q "box-sizing" && head -1 app.js | grep -q "let logoDataUrl" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `styles.css` exists and line 1 contains `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`
- `styles.css` last non-empty line contains `.download-btn:hover { background: #333; }`
- `styles.css` does NOT contain the literal string `<style>` or `</style>`
- `app.js` exists and line 1 contains `let logoDataUrl = null;`
- `app.js` last non-empty line contains `});`
- `app.js` does NOT contain the literal string `<script>` or `</script>`
- `app.js` contains `function getData()`
- `app.js` contains `function update()`
- `app.js` contains `function clearLogo()`
- `app.js` contains `function setFormat(`
- `app.js` contains `function downloadQR()`
- `app.js` does NOT contain `type="module"` or `export` or `import`
  </acceptance_criteria>
  <done>styles.css contains all 221 lines of CSS extracted verbatim; app.js contains all 118 lines of JS extracted verbatim; neither file contains HTML wrapper tags</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html to link external files</name>
  <files>index.html</files>
  <read_first>index.html, styles.css, app.js</read_first>
  <action>
Modify `index.html` to remove inline CSS and JS, replacing them with external file references:

**In the head section — replace the entire inline style block (lines 8-230) with a single link tag:**
```html
  <link rel="stylesheet" href="styles.css">
```
Keep the CDN script tag in head BEFORE the new link tag. The final head must be exactly:
```html
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QR Code Generator</title>
  <script src="https://unpkg.com/qr-code-styling@1.6.0-rc.1/lib/qr-code-styling.js"></script>
  <link rel="stylesheet" href="styles.css">
</head>
```

**Before the closing body tag — replace the entire inline script block (lines 338-457) with:**
```html
  <script src="app.js"></script>
```
The script src="app.js" tag must be the last element before the closing body tag, exactly where the inline script was. Do NOT place it in head. Do NOT add type="module" or defer.

**All HTML markup between the opening body tag and the old script tag (lines 232-336) must remain unchanged.** Do not alter any onclick attributes, element IDs, class names, or structure.

The resulting `index.html` should be approximately 120 lines (markup only, no inline CSS or JS).
  </action>
  <verify>
    <automated>grep -q 'href="styles.css"' index.html && grep -q 'src="app.js"' index.html && ! grep -q 'let logoDataUrl' index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `index.html` contains `link rel="stylesheet" href="styles.css"`
- `index.html` contains `script src="app.js"`
- `index.html` contains `unpkg.com/qr-code-styling` in head
- `index.html` does NOT contain `let logoDataUrl`
- `index.html` does NOT contain `function update()`
- `index.html` still contains `onclick="clearLogo()"`
- `index.html` still contains `onclick="setFormat('png')"`
- `index.html` still contains `onclick="setFormat('svg')"`
- `index.html` still contains `onclick="downloadQR()"`
- `index.html` still contains `id="qr-canvas"`
- The CDN script tag appears BEFORE `href="styles.css"` in the file
- The `src="app.js"` script tag appears AFTER `id="qr-canvas"` in the file
  </acceptance_criteria>
  <done>index.html contains only HTML markup with external links to styles.css and app.js; no inline CSS or JS remains; all onclick handlers and element IDs preserved; CDN script loads before app.js</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all features work in browser</name>
  <action>Human verifies the complete codebase split works correctly by testing all features in a browser.</action>
  <verify>Human confirms all 14 checks pass</verify>
  <done>User approves that the split app behaves identically to the original monolithic index.html</done>
  <what-built>Complete codebase split: index.html markup separated from styles.css and app.js. All three files work together to produce the same QR code generator application.</what-built>
  <how-to-verify>
1. Open `index.html` in a browser (double-click or `open index.html`)
2. Verify QR code renders in preview on page load
3. Type a URL in the URL input field — QR updates live
4. Switch to WiFi tab — fill SSID and password — QR updates
5. Change Dot Shape dropdown — QR updates
6. Change Corner Shape dropdown — QR updates
7. Change Foreground color — QR and hex label update
8. Change Background color — QR and hex label update
9. Click "Choose image" and upload a logo — logo appears in QR center
10. Adjust Logo Size slider — logo size changes
11. Click "remove" — logo clears
12. Click PNG button (should be active by default), click Download — .png file saves
13. Click SVG button, click Download — .svg file saves
14. Open browser console (F12) — verify zero errors
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `test -f styles.css && test -f app.js` — both extracted files exist
- `grep -q 'href="styles.css"' index.html` — CSS link present
- `grep -q 'src="app.js"' index.html` — JS link present
- `grep -qv 'let logoDataUrl' index.html` — no inline JS remains
- `grep -q 'onclick="clearLogo()"' index.html` — onclick handlers preserved
- Manual browser verification confirms zero regressions (checkpoint task)
</verification>

<success_criteria>
- Three files exist: index.html (~120 lines), styles.css (~221 lines), app.js (~118 lines)
- index.html links both external files with correct tags
- CDN script in head loads before app.js before closing body
- All inline onclick handlers still reference global functions in app.js
- Browser loads the app with zero console errors
- All features work: URL QR, WiFi QR, styling controls, color pickers, logo upload/clear/resize, PNG and SVG download
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-split/01-01-SUMMARY.md`
</output>
